<?php require('Views/template/header.phtml'); ?>

    <!-- CSRF: set this somewhere once per session -->
    <meta name="csrf" content="<?= htmlspecialchars($_SESSION['csrf'] ?? '', ENT_QUOTES) ?>">

    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

    <div id="mapApp" data-logged-in="<?= isset($_SESSION['user']) ? '1' : '0' ?>" class="map-container">
        <div class="map-sidebar">
            <h3 class="mb-3">Recent sightings</h3>
            <div class="map-search-wrap">
                <input id="mapSearch" type="text" placeholder="Search by pet, description, user, or pet ID…" class="form-control search-input">
            </div>
            <div class="sort-controls mb-3">
                <span class="sort-label">Sort by:</span>
                <div class="btn-group btn-group-sm" role="group" aria-label="Sort sightings">
                    <button id="sortNewestBtn" type="button" class="btn btn-primary">Newest</button>
                    <button id="sortOldestBtn" type="button" class="btn btn-outline-primary">Oldest</button>
                </div>
            </div>
            <div id="petList"></div>
            <div class="mt-2">
                <button id="loadMoreBtn" type="button" class="btn btn-outline-secondary btn-sm">Load more</button>
            </div>
        </div>

        <div class="map-panel">
            <div id="map" class="map-canvas"></div>
        </div>
    </div>

    <script src="/js/apiClient.js"></script>
    <script src="/js/mapView.js"></script>
    <script src="/js/petListView.js"></script>
    <script src="/js/appMap.js"></script>

<?php require('Views/template/footer.phtml'); ?>
