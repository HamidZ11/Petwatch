<?php require('template/header.phtml'); ?>

    <!-- View file for All Pet Listings page -->
    <!-- Includes filters, sorting, pagination, and responsive layouts -->
    <form method="GET" action="index.php" id="petsSearchForm"
          class="mb-4 d-flex flex-wrap align-items-center gap-2 justify-content-center">
        <input type="hidden" name="page" value="pets">

        <!-- Search keyword -->
        <input type="text" name="search" id="petSearch" class="form-control w-auto"
               placeholder="Search by pet name or pet ID"
               value="<?= htmlspecialchars($_GET['search'] ?? '') ?>">

        <!-- Filter by type -->
        <select name="type" class="form-select w-auto">
            <option value="">All Types</option>
            <?php
            $types = ['Dog','Cat','Rabbit','Bird','Hamster','Fish','Turtle','Other'];
            foreach ($types as $type):
                ?>
                <option value="<?= $type ?>" <?= (($_GET['type'] ?? '') === $type) ? 'selected' : '' ?>>
                    <?= $type ?>
                </option>
            <?php endforeach; ?>
        </select>

        <!-- Filter by status -->
        <select name="status" class="form-select w-auto">
            <option value="">All Status</option>
            <option value="missing" <?= (($_GET['status'] ?? '') === 'missing') ? 'selected' : '' ?>>Missing</option>
            <option value="found" <?= (($_GET['status'] ?? '') === 'found') ? 'selected' : '' ?>>Found</option>
        </select>

        <!-- Age range -->
        <input type="number" name="minAge" class="form-control w-auto"
               placeholder="Min Age" value="<?= htmlspecialchars($_GET['minAge'] ?? '') ?>" min="0">
        <input type="number" name="maxAge" class="form-control w-auto"
               placeholder="Max Age" value="<?= htmlspecialchars($_GET['maxAge'] ?? '') ?>" min="0">

        <!-- Sort options -->
        <select name="sort" class="form-select w-auto">
            <option value="dateAdded DESC" <?= (($_GET['sort'] ?? '') === 'dateAdded DESC') ? 'selected' : '' ?>>Newest First</option>
            <option value="dateAdded ASC" <?= (($_GET['sort'] ?? '') === 'dateAdded ASC') ? 'selected' : '' ?>>Oldest First</option>
            <option value="petID ASC" <?= (($_GET['sort'] ?? '') === 'petID ASC') ? 'selected' : '' ?>>Pet ID (Ascending)</option>
            <option value="petID DESC" <?= (($_GET['sort'] ?? '') === 'petID DESC') ? 'selected' : '' ?>>Pet ID (Descending)</option>
            <option value="name ASC" <?= (($_GET['sort'] ?? '') === 'name ASC') ? 'selected' : '' ?>>Name (A–Z)</option>
            <option value="name DESC" <?= (($_GET['sort'] ?? '') === 'name DESC') ? 'selected' : '' ?>>Name (Z–A)</option>
        </select>

        <!-- Submit -->
        <button type="submit" class="btn btn-primary me-2">Search</button>
        <a href="index.php?page=reportSighting" class="btn btn-outline-success">Report a Sighting</a>
    </form>

    <div id="petsContainer">
    <!-- Pet listings table/card -->
    <div class="card shadow-sm">
        <div class="card-body">
            <h2 class="text-center mb-4">Pet Listings</h2>

            <?php if (!empty($view->petsDataSet)): ?>

                <!-- DESKTOP TABLE VIEW -->
                <div class="table-responsive d-none d-md-block">
                    <table class="table table-striped table-hover align-middle">
                        <thead class="table-dark">
                        <tr>
                            <th>Pet ID</th>
                            <th>Image</th>
                            <th>Name</th>
                            <th>Type</th>
                            <th>Age</th>
                            <th>Description</th>
                            <th>Owner ID</th>
                            <th>Status</th>
                            <th>Date Added</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        <?php foreach ($view->petsDataSet as $pet): ?>
                            <tr>
                                <td><?= htmlspecialchars($pet->getPetID()) ?></td>
                                <td>
                                    <?php if ($pet->getImagePath()): ?>
                                        <img src="<?= htmlspecialchars($pet->getImagePath()) ?>" alt="Pet Image"
                                             width="60" height="60" style="object-fit:cover;border-radius:8px;">
                                    <?php else: ?>
                                        <span class="text-muted">No image</span>
                                    <?php endif; ?>
                                </td>
                                <td><?= htmlspecialchars($pet->getName()) ?></td>
                                <td><?= htmlspecialchars($pet->getType()) ?></td>
                                <td><?= htmlspecialchars($pet->getAge()) ?></td>
                                <td><?= htmlspecialchars($pet->getDescription()) ?></td>
                                <td><?= htmlspecialchars($pet->getOwnerID()) ?></td>

                                <!-- Status badge -->
                                <td>
                                    <?php if (strtolower($pet->getStatus()) === 'missing'): ?>
                                        <span class="badge bg-danger">Missing</span>
                                    <?php elseif (strtolower($pet->getStatus()) === 'found'): ?>
                                        <span class="badge bg-success">Found</span>
                                    <?php else: ?>
                                        <span class="badge bg-secondary"><?= htmlspecialchars($pet->getStatus()) ?></span>
                                    <?php endif; ?>
                                </td>

                                <td><?= htmlspecialchars($pet->getDateAdded() ?? '—') ?></td>

                                <!-- Actions: view, report, edit, delete -->
                                <td>
                                    <?php if ($pet->hasLatestCoords()): ?>
                                        <a href="index.php?page=map&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                           class="btn btn-sm btn-outline-info mb-1">View on Map</a>
                                    <?php endif; ?>
                                    <a href="index.php?page=viewSightings&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                       class="btn btn-sm btn-outline-primary mb-1">View Sightings</a>
                                    <a href="index.php?page=reportSighting&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                       class="btn btn-sm btn-outline-success mb-1">Report Sighting</a>

                                    <?php if (isset($_SESSION['user']) && $_SESSION['user']['userID'] == $pet->getOwnerID()): ?>
                                        <a href="index.php?page=editPet&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                           class="btn btn-sm btn-outline-warning mb-1">Edit</a>
                                        <a href="index.php?page=deletePet&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                           class="btn btn-sm btn-outline-danger"
                                           onclick="return confirm('Are you sure you want to delete this listing?');">Delete</a>
                                    <?php endif; ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>

                <!-- MOBILE CARD VIEW -->
                <div class="d-block d-md-none">
                    <?php foreach ($view->petsDataSet as $pet): ?>
                        <div class="card mb-3 shadow-sm">
                            <div class="card-body">
                                <div class="d-flex align-items-center mb-3">
                                    <?php if ($pet->getImagePath()): ?>
                                        <img src="<?= htmlspecialchars($pet->getImagePath()) ?>" alt="Pet Image"
                                             class="rounded me-3"
                                             style="width: 80px; height: 80px; object-fit: cover;">
                                    <?php endif; ?>
                                    <div>
                                        <h5 class="card-title mb-0"><?= htmlspecialchars($pet->getName()) ?></h5>
                                        <small class="text-muted">
                                            ID #<?= htmlspecialchars($pet->getPetID()) ?> •
                                            <?= htmlspecialchars($pet->getType()) ?> •
                                            Age <?= htmlspecialchars($pet->getAge()) ?>
                                        </small>
                                    </div>
                                </div>

                                <p class="card-text mb-2"><strong>Description:</strong> <?= htmlspecialchars($pet->getDescription()) ?></p>
                                <p class="mb-1"><strong>Owner ID:</strong> <?= htmlspecialchars($pet->getOwnerID()) ?></p>
                                <p class="mb-1"><strong>Date Added:</strong> <?= htmlspecialchars($pet->getDateAdded()) ?></p>

                                <div class="d-flex justify-content-between align-items-center mt-3">
                                    <div>
                                        <?php if (strtolower($pet->getStatus()) === 'missing'): ?>
                                            <span class="badge bg-danger">Missing</span>
                                        <?php elseif (strtolower($pet->getStatus()) === 'found'): ?>
                                            <span class="badge bg-success">Found</span>
                                        <?php else: ?>
                                            <span class="badge bg-secondary"><?= htmlspecialchars($pet->getStatus()) ?></span>
                                        <?php endif; ?>
                                    </div>

                                    <!-- Action buttons -->
                                    <div class="d-flex flex-wrap gap-1">
                                        <?php if ($pet->hasLatestCoords()): ?>
                                            <a href="index.php?page=map&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                               class="btn btn-sm btn-outline-info">Map</a>
                                        <?php endif; ?>
                                        <a href="index.php?page=viewSightings&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                           class="btn btn-sm btn-outline-primary">View</a>
                                        <a href="index.php?page=reportSighting&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                           class="btn btn-sm btn-outline-success">Report</a>
                                        <?php if (isset($_SESSION['user']) && $_SESSION['user']['userID'] == $pet->getOwnerID()): ?>
                                            <a href="index.php?page=editPet&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                               class="btn btn-sm btn-outline-warning">Edit</a>
                                            <a href="index.php?page=deletePet&amp;petID=<?= urlencode($pet->getPetID()) ?>"
                                               class="btn btn-sm btn-outline-danger"
                                               onclick="return confirm('Are you sure you want to delete this listing?');">Delete</a>
                                        <?php endif; ?>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>

            <?php else: ?>
                <p class="text-center text-muted">
                    <?= !empty($_GET['search']) ? 'No results found.' : 'No pets found in the system.' ?>
                </p>
            <?php endif; ?>
        </div>
    </div>

    <!-- Pagination controls -->
<?php if (!empty($view->petsDataSet)): ?>
    <?php
    $currentPage = $view->currentPage ?? 1;
    $totalPages = $view->totalPages ?? 1;
    $baseUrl = "index.php?page=pets";
    $queryParams = $_GET;
    unset($queryParams['pageNum']);
    $queryString = http_build_query($queryParams);

    $buildUrl = function ($pageNum) use ($baseUrl, $queryString) {
        return $baseUrl . ($queryString ? '&' . $queryString : '') . '&pageNum=' . (int)$pageNum;
    };
    ?>
    <div class="d-flex justify-content-center mt-4 align-items-center gap-3 flex-wrap">
        <?php if ($currentPage > 1): ?>
            <a href="<?= $buildUrl($currentPage - 1) ?>" class="btn btn-outline-primary">Previous</a>
        <?php endif; ?>

        <div class="pagination-links">
            <?php
            $start = max(1, $currentPage - 2);
            $end = min($totalPages, $currentPage + 2);
            ?>
            <?php if ($start > 1): ?>
                <a class="page-link" href="<?= $buildUrl(1) ?>">1</a>
                <?php if ($start > 2): ?><span class="page-ellipsis">…</span><?php endif; ?>
            <?php endif; ?>
            <?php for ($i = $start; $i <= $end; $i++): ?>
                <a class="page-link <?= ($i === $currentPage) ? 'active' : '' ?>" href="<?= $buildUrl($i) ?>"><?= $i ?></a>
            <?php endfor; ?>
            <?php if ($end < $totalPages): ?>
                <?php if ($end < $totalPages - 1): ?><span class="page-ellipsis">…</span><?php endif; ?>
                <a class="page-link" href="<?= $buildUrl($totalPages) ?>"><?= $totalPages ?></a>
            <?php endif; ?>
        </div>

        <?php if ($currentPage < $totalPages): ?>
            <a href="<?= $buildUrl($currentPage + 1) ?>" class="btn btn-outline-primary">Next</a>
        <?php endif; ?>

        <form method="GET" class="jump-form d-flex align-items-center gap-2">
            <?php foreach ($queryParams as $k => $v): ?>
                <input type="hidden" name="<?= htmlspecialchars($k) ?>" value="<?= htmlspecialchars($v) ?>">
            <?php endforeach; ?>
            <label class="small text-muted">Page</label>
            <input type="number" min="1" max="<?= (int)$totalPages ?>" name="pageNum" class="form-control form-control-sm" style="width:80px;" value="<?= (int)$currentPage ?>">
            <span class="small text-muted">of <?= (int)$totalPages ?></span>
            <button type="submit" class="btn btn-sm btn-outline-secondary">Go</button>
        </form>
    </div>
<?php endif; ?>
    </div>

<?php require('template/footer.phtml'); ?>
